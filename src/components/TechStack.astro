---
import { skillCategories } from "../data/skills";
---

<section id="skills" class="py-24 relative z-10">
  <div class="max-w-7xl mx-auto px-6">
    <!-- Section Header (Stacked) -->
    <div class="reveal flex flex-col gap-4 mb-16 max-w-2xl">
      <h2 class="text-4xl md:text-5xl font-bold text-slate-100 tracking-tight">
        Toolkit
      </h2>
      <p class="text-slate-400 text-lg">
        The languages, frameworks, and tools I use to build software.
      </p>
    </div>

    <!-- Skill Categories (Minimal layout, no heavy cards) -->
    <div class="grid md:grid-cols-3 gap-12">
      {
        skillCategories.map((category, catIdx) => (
          <div
            class="reveal flex flex-col gap-6"
            style={`animation-delay: ${catIdx * 0.1}s;`}
          >
            <h3 class="text-lg font-semibold text-slate-300 border-b border-slate-800 pb-4">
              {category.title}
            </h3>

            <ul class="flex flex-col gap-4">
              {category.skills.map((skill) => (
                <li class="flex items-center gap-4 group">
                  <div class="w-10 h-10 rounded-lg bg-slate-900/50 border border-slate-800 flex items-center justify-center group-hover:border-emerald-500/30 transition-colors">
                    <img
                      src={`https://skillicons.dev/icons?i=${skill.icon}`}
                      alt={skill.name}
                      width="20"
                      height="20"
                      class="opacity-70 group-hover:opacity-100 transition-opacity"
                      loading="lazy"
                    />
                  </div>
                  <span class="text-sm font-medium text-slate-400 group-hover:text-slate-200 transition-colors">
                    {skill.name}
                  </span>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>
  </div>
</section>
